import 'package:flutter_test/flutter_test.dart';

void main() {
  // Tests de smoke — agregar tests reales aquí conforme crece la app
  testWidgets('App smoke test placeholder', (WidgetTester tester) async {
    expect(true, isTrue);
  });
}
